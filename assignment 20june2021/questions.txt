20 June 2021 assignment    

1. Find the second largest number in the array
2.check if array is sorted in ascending order or not?
3.Left rotate an array upto user's choice 
4. Find maximum sum of sub array                         array[5]={1,-2,3,-1,2},sum=0;    
for(i=0,i<n;i++)
{ 
sum=sum+array[i]; max_sum=max(max_sum, sum);
  }